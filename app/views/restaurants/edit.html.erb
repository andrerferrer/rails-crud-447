<%# restaurant_in_the_partial is the @restaurant that I received from the controller %>

<%= render 'form', restaurant_in_the_partial: @restaurant %>
<%# the render method is going to take a partial name as an arg AND the local variables inside my partial %>